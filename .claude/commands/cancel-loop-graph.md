# Cancel Loop Graph

Cancel the active Graph Engineering Loop run in the current project.

```bash
node packages/cli/dist/cli.js cancel
```

Report whether a run was cancelled or no active run was found.
