{
  "name": "graph-engineering-loop-repo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/core packages/cli",
    "test": "node --test --import tsx tests/**/*.test.ts",
    "typecheck": "tsc -b packages/core packages/cli --pretty false && tsc -b packages/core packages/cli --clean"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
