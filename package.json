{
  "name": "graph-engineering-loop-repo",
  "version": "0.2.0",
  "private": false,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/core packages/cli",
    "build:plugin": "npm run build && node scripts/build-plugin.mjs",
    "clean": "tsc -b packages/core packages/cli --clean && rm -rf packages/core/dist packages/cli/dist",
    "prepack": "npm run clean && npm run build",
    "pack:dry": "npm run clean && npm run build && npm --cache .npm-cache pack --workspace graph-engineering-loop-core --dry-run && npm --cache .npm-cache pack --workspace graph-engineering-loop --dry-run",
    "verify:release": "node scripts/verify-release-version.mjs",
    "smoke:claude": "node scripts/smoke-claude.mjs",
    "smoke:plugin": "npm run build:plugin && node scripts/smoke-plugin.mjs",
    "smoke:plugin:claude": "node scripts/smoke-plugin-claude.mjs",
    "smoke:fake": "npm run build && node packages/cli/dist/cli.js run examples/fake/loops.json --adapter fake",
    "test": "node --test --import tsx tests/**/*.test.ts",
    "typecheck": "tsc -b packages/core packages/cli --pretty false && tsc -b packages/core packages/cli --clean"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
