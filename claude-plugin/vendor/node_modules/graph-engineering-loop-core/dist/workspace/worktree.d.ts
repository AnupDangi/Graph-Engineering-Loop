export interface WorktreeInfo {
    loopId: string;
    branch: string;
    path: string;
    createdAt: string;
}
export interface WorktreeManagerOptions {
    gitPath?: string;
}
export declare class WorktreeManager {
    readonly projectRoot: string;
    readonly workspacesDir: string;
    private readonly git;
    constructor(projectRoot: string, options?: WorktreeManagerOptions);
    create(loopId: string): Promise<WorktreeInfo>;
    list(): Promise<WorktreeInfo[]>;
    remove(loopId: string): Promise<void>;
    removeAll(): Promise<void>;
    readInfo(loopId: string): Promise<WorktreeInfo | null>;
    private writeInfo;
    private readDir;
    private ensureGitRepo;
}
export declare function isGitRepository(projectRoot: string, gitPath?: string): Promise<boolean>;
export declare function currentBranch(projectRoot: string, gitPath?: string): Promise<string>;
