import type { LoopGraph, LoopResult } from "../schema/types.js";
import { type WorktreeInfo, type WorktreeManager } from "./worktree.js";
export type IntegrationStepStatus = "pending" | "merged" | "failed" | "skipped";
export interface IntegrationStep {
    loopId: string;
    branch: string;
    status: IntegrationStepStatus;
    message?: string;
}
export interface IntegrationPlan {
    baseBranch: string;
    steps: IntegrationStep[];
}
export interface IntegrationSupervisorOptions {
    gitPath?: string;
}
export declare class IntegrationSupervisor {
    private readonly worktreeManager;
    private readonly git;
    constructor(worktreeManager: WorktreeManager, options?: IntegrationSupervisorOptions);
    buildPlan(graph: LoopGraph, results: LoopResult[], worktrees: WorktreeInfo[]): Promise<IntegrationPlan>;
    integrate(graph: LoopGraph, results: LoopResult[]): Promise<IntegrationPlan>;
    private commitWorktree;
    finalize(graph: LoopGraph, results: LoopResult[]): Promise<IntegrationPlan>;
}
