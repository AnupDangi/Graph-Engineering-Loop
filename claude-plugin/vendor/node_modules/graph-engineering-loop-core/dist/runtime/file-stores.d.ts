import type { GraphRunResult, GraphRunSnapshot, GraphRuntimeHooks } from "./graph-runtime.js";
import type { GraphStatus, LoopGraph, LoopResult } from "../schema/types.js";
export interface RunMetadata {
    runId: string;
    graphName: string;
    graphHash: string;
    projectRoot: string;
    startedAt: string;
}
export interface StateFile {
    version: 1;
    runId: string;
    graphName: string;
    graphHash: string;
    status: GraphStatus;
    startedAt: string;
    updatedAt: string;
    loops: GraphRunSnapshot["loops"];
}
export interface LockFile {
    runId: string;
    pid: number;
    startedAt: string;
    projectRoot: string;
}
export declare class LoopGraphFiles {
    readonly projectRoot: string;
    readonly loopgraphDir: string;
    readonly resultsDir: string;
    readonly statePath: string;
    readonly lockPath: string;
    readonly eventsPath: string;
    constructor(projectRoot: string);
    ensure(): Promise<void>;
    writeState(metadata: RunMetadata, snapshot: GraphRunSnapshot | GraphRunResult): Promise<void>;
    writeRawState(state: StateFile): Promise<void>;
    createLock(metadata: RunMetadata): Promise<void>;
    removeLock(): Promise<void>;
    appendEvent(type: string, payload: Record<string, unknown>): Promise<void>;
    writeLoopResult(result: LoopResult): Promise<void>;
    readState(): Promise<StateFile | null>;
    hooks(metadata: RunMetadata): GraphRuntimeHooks;
    private persistLoopEvent;
}
export declare function createRunMetadata(graph: LoopGraph, projectRoot: string): RunMetadata;
export declare function hashGraph(graph: LoopGraph): string;
export declare function isTerminalGraphStatus(status: GraphStatus): boolean;
