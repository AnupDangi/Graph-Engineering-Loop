export interface PlannedWriteSet {
    loopId: string;
    files: string[];
}
export interface OverlapDecision {
    loopA: string;
    loopB: string;
    sharedFiles: string[];
    overlapRatio: number;
    serialized: boolean;
}
export interface WavePlan {
    waves: string[][];
    serializations: OverlapDecision[];
}
export declare const DEFAULT_MAX_OVERLAP_RATIO = 0.25;
export declare function assessOverlap(a: PlannedWriteSet, b: PlannedWriteSet, maxOverlapRatio?: number): OverlapDecision;
export declare function planExecutionWaves(plans: PlannedWriteSet[], maxConcurrent: number, maxOverlapRatio?: number): WavePlan;
export declare function isOverlappingSet(loopIds: string[], plans: PlannedWriteSet[], maxOverlapRatio?: number): boolean;
